import {Component, OnInit} from "@angular/core";
@Component({
  moduleId: module.id,
  selector: 'map',
  template:'<div></div>'
})
export class MapComponent implements OnInit {
  ngOnInit(): void {
  }
}
