export class Wifi {
  id: number;

  coordinates: {
    lat: number;
    lng: number;
  };

  location: {
    name: string;
    street: string;
    nr: string;
    code: string;
    city: string;
  };
}
